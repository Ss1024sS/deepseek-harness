(version 1)
(allow default)

; This process is a keyless replay sidecar. A later cloud provider profile must
; replace this rule deliberately and keep a destination allowlist outside Cordis.
(deny network*)

; The runtime may persist its append-only Session log and nothing else.
(deny file-write*
  (require-not (subpath (param "SESSION_ROOT"))))
